spring:
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true


  jpa:
    hibernate:
      ddl-auto: create

    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true
    defer-datasource-initialization: true


  datasource:
    username: ENC(2SkZyC/z/C0Um0i4IOtuAA==)
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always
      data-locations: classpath:testdata/initial_category_brand_product.sql


logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: trace
